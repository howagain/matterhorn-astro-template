---
import Layout from "../../layouts/Layout.astro";
import DealerApp from "@components/DealerApp";;

const { id } = Astro.params;
const dealerId = String(id || "");
---

<Layout title={`Dealer ${dealerId} – Coupon`}>
  <style>
    .page { max-width: 900px; margin: 2rem auto; padding: 0 1rem; }
  </style>

  <DealerApp dealerId={dealerId} client:only="solid-js" />
</Layout>

{/* Solid island now provided by external component */}

